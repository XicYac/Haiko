        var banda = prompt("Que banda conoces?", "")
        if(banda !=null){
        alert("La banda mencionada es: " + banda)
        document.write(banda, "<br/>")
            if(banda =="Nirvana"){
                document.write("<br/>Nirvana es una banda")
